'use client';

import React from 'react';

export default function ESGOverview() {
  return (
    <section className="flex flex-col items-center text-center space-y-6">
      <h2 className="text-3xl font-bold text-gray-800">ESG란?</h2>
      <p className="max-w-3xl text-gray-600 text-lg leading-relaxed">
        ESG는 기업의 지속 가능한 성장을 위해 환경(Environment), 사회(Social), 지배구조(Governance)를
        통합적으로 고려하는 경영 전략입니다. 정채움은 ESG를 통해 모두가 함께 성장하는 미래를 만들어갑니다.
      </p>
    </section>
  );
}
